# Closed C2 on-policy refresh

CPU-only preparation from all five completed rounds of `timing500-ppo-live-20260924-r1`. The cohort closed at 2026-09-24T11:00:34.706Z with 2 wins, 3 losses and 49:55 points. Nine incomplete attempts are excluded. All completed wins and losses are retained. Minimum5 is an explicit development-training override; this is not a passed evaluation or a large independent sample.

Actual behavior checkpoint:
`c2c4987b268996cd912fe35e6ba5f9b20a94d69fd93e4ef15b6c5fc71ee5e533`

Path: `/home/spark-advantage/rek-training/timing500-onpolicy-20260924-r2/train-score-delta/policy.bin`.

This data was generated by C2, not by the original5b19 checkpoint or the currently evaluated7001 LR sibling. There is no teacher migration and no reuse of another checkpoint's behavior probabilities. The original worker logs do not contain logits; exact native replay from C2 and the actual per-round seeds is required before any optimizer update.

| Seed / completed attempt | Points | Recorded decisions |
| --- | --- | ---: |
| 1301 / retry7 | 2:6 | 3005 |
| 1302 / retry2 | 9:11 | 3090 |
| 1303 / retry3 | 20:14 | 2958 |
| 1304 | 12:10 | 2838 |
| 1305 | 6:14 | 3066 |
| Total | 49:55 | 14957 |

All full223 inputs, legal masks, sampled actions, actual seeds1301..1305, source QPC intervals, reset/order, and terminal rejected requests are preserved. Expected applied actor rows are14952 with five terminal-race actor-weight-zero rows. Those final rows remain in recurrent history and return propagation. No cadence conversion, omitted decision, feature masking, guessed fall event, reward bonus or attack gate is introduced.

## Reused derivation and exact native captures

This is a bounded copy of the tested timing500-onpolicy r2 CPU pipeline. Changes are the C2 checkpoint pin, closed-five-round selector, explicitly pinned native capture selection, and LR3e-5 native command setting. The balance8 exporter, controlled-start receipt, original reward export and point-delta derivation otherwise retain the same implementation. Native CUDA binaries are reused unchanged.

The exact native capture list is:
`/home/spark-advantage/rek-training/timing500-loss-analysis-20260924-r1/native-captures-final.json`

SHA256: `7f3189c1d619f7494eaa6f611eedcc850152d387f1a02bb0f74e8bcf9d516cf1`.

Each selected episode binds to its listed native path, byte count and SHA rather than selecting a file by timestamp. All packet, referee, received-score, identity and terminal checks are rerun. The unchanged contact analyzer's legacy coverage result is preserved; the separately versioned controlled-start receipt reapplies the existing one-second ACK bounds using UnityTime. Startup identity and reward discount use QPC. Actual initial timers and uncontrolled startup spans are retained. Both observed counters must stay zero through the first worker decision.

The baselinev3 export retains the original terminal-outcome plus score-potential metadata. The derivedv5 dataset changes only reward and gamma cells:

```text
r = ((nextOwn-own) - (nextOpponent-opponent))/5
gamma = float32(2^(-actual_QPC_dt/5))
G = r + (terminal ? 0 : gamma*nextG)
advantage = return = G
```

No terminal bonus, score potential or clipping is added to v5 rewards. Undiscounted per-round rewards must equal the actual point margins divided by5: -0.8,-0.4,1.2,0.4,-1.6, within float32 tolerance.

## CPU preparation

From `/home/spark-advantage/rek-training/timing500-ppo-refresh-20260924-r1`:

```sh
node --test onpolicy.test.cjs controlled_policy_coverage.test.cjs test_score_delta.cjs source/authentic_trajectory_data.test.cjs source/authentic_trajectory_v3.test.cjs
node select_closed_cohort.cjs
node prepare_export.cjs selection.json --run
```

The selector verifies the closed ledger/end event and all five exact terminal scores. Existing output files/directories are refused. Raw captures, original summaries, previous stages and active cohorts are not changed. The copied native list is included for local CPU tests; actual preparation pins the authoritative frozen list above.

## Prospective native execution, not run by preparation

```sh
node run_native.cjs run-plan.json replay --check
node run_native.cjs run-plan.json train --check
```

After explicit review and a free GPU window, the existing runner can execute:

```sh
node run_native.cjs run-plan.json replay --run
node run_native.cjs run-plan.json train --run
```

Replay must reproduce every one of the14957 recorded C2 samples with the actual worker52741, native object4ada3, all-ones mask59158 and balance8 schema. Training is planned as one fresh epoch from C2, LR3e-5, H128, clip0.2, VF clip0.2, VF0, entropy0.001, complete-MC zero baseline, unchanged BF16 distributional acceptance and true frozen old probabilities. Expected updates are120. Final epoch1 is fixed prospectively, with no development-selected checkpoint. No live candidate or live-control preparation is part of this stage.

The plan is prepared for review only. GPU replay and training remain unexecuted; preparing fresh data does not establish improved fighting or authorize replacing the active7001 candidate.

## Executed CPU result

Actual all-five export exited 0. All 40 CPU tests passed on both Windows and Spark. Every explicit native capture SHA/size, native/referee validation and controlled-coverage receipt passed. The unchanged legacy coverage flags remain four false and one true. Export contains all 14,957 decisions with 14,952 applied actor rows and five terminal-rejected actor-weight-zero rows. Observed score totals are 49:55; each round's reward sum equals its measured point margin divided by 5 within float32 tolerance. Native replay and training `--check` passed without GPU execution.

- Dataset SHA256: `eb6b1ae210b3b3911597f93a513ae32081f15e4871b29c1cb29e75829f718655`.
- Identity SHA256: `5e65d45f54e444a47d5f87e10691715ac1b0bc82a44685dd015e0e9efdb333c0`.
- Run-plan SHA256: `5e237a46193220e1b825afb9da5f6ec08565603f6c13ebd99ed786618b8f9e26`.
- Evidence-manifest SHA256: `efc7c7a9a7e1c030450712a83302742d5a7e97818f5b3d7da31beaaf4b32fb54`.

See `CPU-EXPORT-RECEIPT.json` for actual per-round cadence, QPC spans, coverage and rewards. GPU replay, optimizer updates and live deployment remain unexecuted.
