import torch
import pufferlib.models

# Import the default policy from pufferlib.models
Policy = pufferlib.models.Default

# Import the LSTM wrapper from pufferlib.models
Recurrent = pufferlib.models.LSTMWrapper